{
  "name": "meu-json-server",
  "version": "1.0.0",
  "description": "API fake com json-server",
  "main": "index.js",
  "scripts": {
    "start": "json-server --watch db/db.json --port 3000"
  },
  "dependencies": {
    "json-server": "^0.17.3"
  }
}